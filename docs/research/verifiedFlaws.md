[
  {
    "question_text": "Name two techniques to reduce hallucinations in an LLM application.",
    "reason": "Internal contradiction: expected_answer accepts 'lowering temperature' as a valid hallucination mitigation, but the bank's own Q on RAG-vs-fine-tune-vs-temperature states 'Lowering temperature only reduces randomness, not factual grounding.' A self-grader will get conflicting signals; temperature reduction is at best a weak/contested mitigation and should not be listed as a canonical correct option.",
    "verdict": "correct",
    "explanation": "No genuine contradiction exists. The two questions address different scopes: the flagged question asks students to name general hallucination-mitigation techniques, and lowering temperature is a legitimate (if secondary) mitigation for sampling-induced confabulation — higher temperature demonstrably increases unfaithful/spurious content in factual tasks, and low temperature for fact-based output is standard guidance across vendor documentation including NVIDIA's. The RAG-vs-fine-tune-vs-temperature question (lines 378-383) is a scenario about knowledge-gap hallucination (facts about current internal policies the model cannot know), where its statement 'lowering temperature only reduces randomness, not factual grounding' is correct and correctly scoped — temperature cannot fix missing knowledge, but that does not negate its effect on randomness-driven hallucination. No self-grader conflict arises in practice: 'lowering temperature' is accepted in the name-two-techniques question and correctly rejected in the scenario question, which are consistent judgments on different questions. The rubric also lists temperature as merely one of five acceptable options, not a canonical best answer; removing it would penalize a widely taught, technically defensible response on a recall-level entry-cert item. The auditor's characterization of temperature as 'weak/partial' is fair, but weak is not wrong, and the bank as written teaches the correct nuance.",
    "suggested_fix": ""
  },
  {
    "question_text": "What is a Mixture-of-Experts (MoE) architecture, and what is its main advantage for scaling large language models?",
    "reason": "Misleading claim in expected_answer: 'giving higher quality at lower per-token FLOPs than a dense model of equal size.' A dense model of equal TOTAL parameter count generally matches or exceeds MoE quality (at far higher compute). The correct framing is quality-per-FLOP: MoE beats a dense model of equal COMPUTE, or delivers big-model quality at small-model per-token cost. As written it teaches a wrong comparison.",
    "verdict": "flawed",
    "explanation": "The auditor's concern is technically valid. The expected_answer's clause \"giving higher quality at lower per-token FLOPs than a dense model of equal size\" claims MoE dominates an equal-parameter dense model on both quality and compute. The FLOPs half is true, but the quality half is wrong: the MoE literature (GShard, Switch Transformer, GLaM, Mixtral, DeepSeek-MoE) consistently shows that at equal TOTAL parameter count a dense model — which applies all parameters to every token — generally matches or exceeds MoE quality, just at far higher compute. MoE is less parameter-efficient but more compute-efficient. The correct framing of its advantage is quality-per-FLOP: MoE beats a dense model of equal COMPUTE (equal active parameters per token), or delivers big-dense-model quality at small-model per-token cost — Switch Transformer's headline result was exactly this equal-FLOPs comparison. As written the answer teaches the wrong comparison baseline, and since it is the grading rubric, it could penalize students who give the correct equal-compute framing. The rest of the answer (sparse top-k routing, capacity growth at constant per-token compute, memory and load-balancing trade-offs) is accurate; only the one clause needs repair. Location: c:/Code/LLM/LLMStudy/db/seed/nca-genl.json, line 345.",
    "suggested_fix": "Replace the expected_answer with: \"MoE replaces dense feedforward layers with many expert sub-networks plus a gating/router that activates only a small subset (top-k) of experts per token. This sparse activation lets total parameter count (capacity) grow very large while compute per token stays roughly constant, giving higher quality per FLOP: an MoE outperforms a dense model of equal per-token compute (equal active parameters), or delivers the quality of a much larger dense model at a fraction of the per-token cost. The trade-off is high memory to hold all experts plus routing and load-balancing complexity.\""
  },
  {
    "question_text": "In a semantic-search / RAG retriever, why is cosine similarity typically used to compare a query embedding with document embeddings rather than raw Euclidean distance, and what do cosine values near 1, near 0, and negative indicate?",
    "reason": "Self-undercutting and partially overclaimed: the answer itself concedes that on L2-normalized vectors (standard practice for most embedding models) cosine and Euclidean ranking coincide, dissolving the question's premise. 'Negative = opposing meaning' is also an overclaim — negative cosine in real embedding spaces does not reliably indicate antonymy/opposition (many models produce anisotropic, mostly non-negative similarity ranges).",
    "verdict": "flawed",
    "explanation": "The auditor's first concern (self-undercutting) is overreach: the question asks about RAW Euclidean distance, i.e., unnormalized vectors, where the magnitude-invariance rationale fully holds (embedding norms vary with text length/token frequency and contaminate raw L2 distance). The parenthetical noting that cosine and Euclidean rankings coincide on L2-normalized vectors is a correct nuance, not a contradiction — it explains why the metric choice is low-stakes after normalization, and cosine genuinely is the typical/default metric in semantic search. Normalization is also common but not universal, so the premise is not dissolved. However, the second concern identifies a real technical error: 'negative = opposing meaning' is false as a claim about real text-embedding spaces. Antonyms and semantically opposing statements typically have HIGH positive cosine similarity because they occur in similar contexts, and modern embedding spaces are anisotropic (vectors occupy a narrow cone), so pairwise similarities are almost entirely non-negative; a negative value, when it occurs, indicates strong dissimilarity or noise, not semantic opposition. The correct statement is geometric ('vectors point in opposing directions = strongly dissimilar'), with the antonymy reading explicitly disclaimed. Only that one clause needs fixing; the question wording and the rest of the expected_answer are sound.",
    "suggested_fix": "Keep the question_text as is; revise expected_answer to: \"Cosine similarity measures the angle between two vectors and ignores their magnitude, so it captures semantic orientation regardless of vector length (embedding norms vary with text length/token frequency, which raw Euclidean distance would conflate with meaning). Near 1 = vectors point almost the same direction = semantically very similar; near 0 = nearly orthogonal = unrelated; negative = vectors point in opposing directions = strongly dissimilar. Caveat: with real text-embedding models the space is anisotropic and similarities are mostly non-negative, so a negative value should be read as 'very dissimilar,' not as reliable evidence of opposite/antonymous meaning — antonyms often score high because they appear in similar contexts. Embeddings are often L2-normalized, in which case cosine and Euclidean nearest-neighbor rankings coincide; the cosine rationale matters most for unnormalized vectors.\""
  },
  {
    "question_text": "A bank uses an LLM to pre-screen loan applications and wants to check whether approval rates differ unfairly across protected groups. Which metric best quantifies this, and what threshold is commonly used as a red flag?",
    "reason": "The four-fifths (80%) rule is US EEOC employment-selection guidance, not a lending-regulation standard; presenting 0.8 as 'the' commonly used red flag for loan approvals is jurisdiction- and domain-fragile trivia. 'Which metric best quantifies this' is also ambiguous — demographic parity difference or standardized mean difference are equally defensible answers a self-grader might wrongly mark down.",
    "verdict": "correct",
    "explanation": "The expected answer (disparate/adverse impact ratio with the four-fifths 0.8 red-flag threshold) is the canonical answer in AI bias-detection practice and is not misleading as written. The auditor's regulatory objection misreads the question: it asks what threshold is \"commonly used as a red flag,\" not what lending law mandates. The 0.8 disparate impact ratio is exactly the common red-flag heuristic in fairness toolkits and courseware — IBM AIF360 documents the 0.8 threshold and applies it to loan-approval examples (German Credit dataset), and fair-lending analysts use adverse impact ratios as screening statistics — so its EEOC employment-guidance origin does not make the answer wrong in this context. The expected answer is also properly hedged (\"signals potential adverse impact... warrants investigation\"), claiming a heuristic trigger rather than a legal standard. The ambiguity objection also fails: the question's requirement to pair the metric with a commonly used threshold effectively forces the ratio form, since only the disparate impact ratio has a canonical, widely cited cutoff (0.8); statistical parity difference has no comparably famous threshold, and standardized mean difference applies to continuous outcomes, not binary approval rates. A self-grader comparing against this expected answer faces low mismatch risk.",
    "suggested_fix": ""
  },
  {
    "question_text": "Your LLM-powered app intermittently returns malformed JSON that breaks downstream parsing. Which Python-library approach most reliably enforces a valid, typed output structure, and why is it better than prompt tweaking alone?",
    "reason": "Expected_answer contains a non-sequitur: 'more reliable than raising temperature or regex-scraping free text' — nobody proposes RAISING temperature to fix malformed JSON (likely a typo for lowering), and the question asked for a comparison against 'prompt tweaking alone,' which the answer never actually addresses. The core recommendation (Pydantic/instructor/JSON mode) is sound, but the justification clause misleads.",
    "verdict": "flawed",
    "explanation": "The core recommendation in expected_answer (Pydantic model + PydanticOutputParser/instructor backed by native function-calling or JSON mode, with validation and retry) is technically sound. However, the justification clause contains a real error: \"more reliable than raising temperature or regex-scraping free text.\" Raising temperature increases sampling randomness and makes malformed JSON MORE likely — nobody proposes it as a fix; the common naive mitigation is LOWERING temperature, so this is almost certainly a typo, and as written it misleads (a grader anchored to this key could penalize a student who correctly names \"lowering temperature\" as the weak alternative). Additionally, the question explicitly asks why the approach beats \"prompt tweaking alone,\" but the answer's named comparanda are temperature and regex-scraping instead. The auditor slightly overstates this second point — the closing phrase \"enforced and checked rather than merely requested\" does implicitly capture the prompt-tweaking rationale — but combined with the temperature error, the justification sentence fails as a grading key and should be corrected. Location: c:\\Code\\LLM\\LLMStudy\\db\\seed\\nca-genl.json, lines 396-401.",
    "suggested_fix": "Replace the final sentence of expected_answer with: \"It is more reliable than prompt tweaking alone — or ad-hoc fixes like lowering temperature or regex-scraping free text — because prompts can only request structure, whereas schema-backed parsing enforces and validates it programmatically, with automatic retry or repair when the model deviates.\" (Question wording is fine as-is.)"
  },
  {
    "question_text": "Your assistant must fetch a user's live account balance from an internal REST API. Which LLM-integration mechanism lets the model invoke that API reliably, and how does the control flow actually work?",
    "reason": "Overclaim in expected_answer: tool/function calling 'keeping execution deterministic and secure.' The mechanism guarantees neither — the model's choice of function and generated arguments remain non-deterministic and injectable; security depends entirely on app-side validation and least-privilege, which the answer does not mention. The control-flow description is otherwise correct.",
    "verdict": "flawed",
    "explanation": "The mechanism identification (function/tool calling) and the control-flow description (declare function with name/description/JSON schema; model emits a structured call; app code executes it and returns the result to the model) are correct. However, the closing clause \"keeping execution deterministic and secure\" is a genuine technical overclaim, and it is framed as the crucial takeaway. (1) Determinism: whether the model emits a tool call, which function it selects, and the arguments it generates are sampled outputs — non-deterministic and potentially malformed or hallucinated; only the app-side function body is deterministic, and it runs on non-deterministic inputs. The claim also contradicts the very next question in the same seed file (line 434), which teaches that LLM outputs are non-deterministic. (2) Security: model-generated arguments are injectable via prompt injection (OWASP LLM Top 10), and blindly executing them in trusted app code is the classic confused-deputy/excessive-agency vulnerability — e.g., executing get_balance with a model-supplied account_id for another user. What tool calling actually provides is application control (credential isolation, an allowlisted function surface, and the app's ability to validate/authorize before executing), not security or determinism per se. The auditor mildly overstates (\"entirely\" app-side — the mechanism does contribute credential isolation and a restricted function surface), but the core objection is technically sound. As ground-truth text in a study app, the unqualified clause would teach a real misconception; only that clause needs revision.",
    "suggested_fix": "Replace the expected_answer with: \"Function/tool calling: you declare the function with a name, description, and a JSON schema of parameters; when appropriate the LLM returns a structured call (function name plus arguments), which your application code executes and then feeds the result back to the model. Crucially, the LLM never calls the API itself and never holds credentials; it only emits a structured request, keeping execution under your application's control. Because the chosen function and its arguments are still model-generated (non-deterministic and susceptible to prompt injection), the app must validate arguments and enforce authorization/least privilege before executing the call.\""
  },
  {
    "question_text": "You are building an application whose primary job is to index a large corpus of internal documents and answer questions over them via retrieval. Between LangChain and LlamaIndex, which is the more purpose-built choice, and what is the key distinction?",
    "reason": "Time-sensitive, ecosystem-opinion framing with no stable single right answer: the 2023-era 'LlamaIndex = RAG, LangChain = orchestration' distinction has blurred (LangChain has mature retrieval abstractions; LlamaIndex ships agents). Defensible as a study heuristic, but a learner answering 'either, they overlap heavily' would be substantively correct yet fail self-grading against the expected_answer.",
    "verdict": "correct",
    "explanation": "The expected_answer contains no technical error. The question asks which framework is \"the more purpose-built choice\" for a retrieval-centric document-indexing app — a question about design specialization with a stable answer (LlamaIndex, originally GPT Index, built around data connectors, index structures, and query engines), not a claim that LangChain cannot do RAG. The expected_answer explicitly acknowledges the overlap the auditor worries about (\"LangChain... can do RAG but is more general-purpose\") and even notes interoperability (\"the two can also interoperate\"). This LlamaIndex-for-retrieval / LangChain-for-orchestration distinction is exactly what NCA-GENL-aligned study materials teach, so it is exam-appropriate. A learner who answers only \"either, they overlap heavily\" has not answered the question actually asked (which is MORE purpose-built), so being marked down is correct grading, not a question flaw. The auditor identifies real ecosystem convergence but cannot point to any false statement in the expected_answer.",
    "suggested_fix": ""
  }
]