# API server port (the web dev server proxies /api to this)
PORT=3001

# Optional: override where the SQLite file lives.
# Defaults to ./db/data/study.db at the repo root.
# DB_PATH=./db/data/study.db
